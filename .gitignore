# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
# Test / type / lint caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
# Env (bí mật — không commit)
.env
.env.*
# uv.lock sống ở repo cha (agentcore-studio-kit), KHÔNG ở submodule này

# `LLMJudge` cap/cache state khi dev override đường dẫn về repo-local (`apps/studio#20`).
state/
